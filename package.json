{
  "name": "atalaydoganay-com",
  "version": "0.1.0",
  "private": true,
  "description": "Personal portfolio site for Atalay Doganay.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs && node serve.mjs",
    "dev": "node build.mjs --watch --serve",
    "test": "node tools/check_escaping.mjs",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "engines": {
    "node": ">=20"
  }
}
